@model IEnumerable<Order_Test.Models.Client>

@if (Model.Count() > 0)
{
    @foreach (var item in Model)
    {
        <span>@item.FirstName</span>
        <span>@item.LastName</span>
    }
}
else
{
        <span>no entries </span>
}
