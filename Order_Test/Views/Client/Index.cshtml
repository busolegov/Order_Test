@model IEnumerable<Order_Test.Models.Client>

@if (Model.Count() > 0)
{
    @foreach (var item in Model)
    {
        <a class="nav-link text-dark" asp-area="" asp-controller="Order" asp-action="Index">@item.FirstName @item.LastName</a>
    }
}
else
{
    <span>no entries</span>
}

